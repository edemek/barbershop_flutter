/// List of Enums
/// They cannot be created inside a class or a function
library;

enum TextSizes {
  small,
  medium,
  large,
}

enum OrderStatus {
  processing,
  shipped,
  delivered,
}

enum PaymentMethod {
  cash,
  card,
  paypal,
  visa,
  mastercard,
  googlepay,
  applepay,
  creditcard,
  paytm,
}
